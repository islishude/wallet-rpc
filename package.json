{
  "name": "wallet-rpc",
  "version": "1.2.9",
  "description": "multi-wallet RPC client for crypto coins by TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist types && tsc",
    "test": "ts-node test/*.ts",
    "pub": "git push --all && git push --tags && npm publish"
  },
  "keywords": [
    "Bitcoin",
    "DKKToken",
    "Ethereum"
  ],
  "author": {
    "email": "lishude@foxmail.com",
    "name": "lishude"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "types": "types/index.d.ts",
  "devDependencies": {
    "@types/node": "^9.6.2",
    "tslint": "^5.9.1",
    "tslint-config-prettier": "^1.12.0",
    "typescript": "^2.8.1"
  }
}
