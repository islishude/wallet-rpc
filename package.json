{
  "name": "multi-wallet-rpc",
  "version": "1.0.3",
  "description": "multi-wallet RPC by TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist types && tsc",
    "test": "npm run build && node dist/test/dkkt/index.js",
    "publish": "git push --all && git push --tags && npm publish"
  },
  "keywords": [
    "bitcoin",
    "dkkt"
  ],
  "author": {
    "email": "lishude@foxmail.com",
    "name": "lishude"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "types": "types/index.d.ts",
  "devDependencies": {
    "@types/node": "^9.6.2",
    "typescript": "^2.8.1"
  }
}
