{
  "name": "wallet-rpc",
  "version": "5.0.2",
  "description": "JSONRPC2.0 client with CLI of crypto-currency by TypeScript",
  "main": "dist",
  "types": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tslint -p tsconfig.json && del-cli dist && tsc",
    "test": "ts-node test/*.ts",
    "prerelease": "npm run build && npm test",
    "release": "git push --all && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/islishude/wallet-rpc"
  },
  "homepage": "https://github.com/islishude/wallet-rpc#readme",
  "bin": {
    "wallet-rpc": "dist/bin/cli.js"
  },
  "keywords": [
    "Bitcoin",
    "Ethereum",
    "USDT",
    "OmniLayer",
    "EOS",
    "JSONRPC"
  ],
  "author": {
    "email": "lishude@foxmail.com",
    "name": "lishude"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.15",
    "del-cli": "^1.1.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  },
  "engines": {
    "node": ">=8.9.0"
  }
}
